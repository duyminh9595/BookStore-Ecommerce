import { QuantitybookWithCategory } from './quantitybook-with-category';

describe('QuantitybookWithCategory', () => {
  it('should create an instance', () => {
    expect(new QuantitybookWithCategory()).toBeTruthy();
  });
});

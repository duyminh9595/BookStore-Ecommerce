import { DataCatQuantityPub } from './data-cat-quantity-pub';

describe('DataCatQuantityPub', () => {
  it('should create an instance', () => {
    expect(new DataCatQuantityPub()).toBeTruthy();
  });
});

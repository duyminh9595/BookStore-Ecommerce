import { DataCatQuantityBook } from './data-cat-quantity-book';

describe('DataCatQuantityBook', () => {
  it('should create an instance', () => {
    expect(new DataCatQuantityBook()).toBeTruthy();
  });
});
